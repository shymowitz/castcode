import { TestBed } from '@angular/core/testing';

import { DeviceAccessoryDetailsComponent } from './device-accessory-details.component';

describe('device-accessory-details Component', () => {
    beforeEach(() => {
        TestBed.configureTestingModule({
            declarations: [DeviceAccessoryDetailsComponent]
        });
    });

    it('should ...', () => {
      //  const fixture = TestBed.createComponent(DeviceAccessoryDetailsComponent); //we need to come back and fix (102816)
      //  fixture.detectChanges();
        // expect(fixture.nativeElement.children[0].textContent).toContain('device-accessory-details Works!');
    });

});
