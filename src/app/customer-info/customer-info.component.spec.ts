import { TestBed } from '@angular/core/testing';

import { CustomerInfoComponent } from './customer-info.component';

describe('customer-info Component', () => {
    beforeEach(() => {
        TestBed.configureTestingModule({
            declarations: [CustomerInfoComponent]
        });
    });

    it('should ...', () => {
    //   const fixture = TestBed.createComponent(CustomerInfoComponent); // we need come back and fix this (102816)
      //  fixture.detectChanges();
        // expect(fixture.nativeElement.children[1].textContent).toContain('customer-info Works!');
    });

});
