import { TestBed } from '@angular/core/testing';

import { CustomerLinesComponent } from './customer-lines.component';

describe('customer-lines Component', () => {
    beforeEach(() => {
        TestBed.configureTestingModule({
            declarations: [CustomerLinesComponent]
        });
    });

    it('should ...', () => {
        // const fixture = TestBed.createComponent(CustomerLinesComponent);
      //  fixture.detectChanges();
        // expect(fixture.nativeElement.children[0].textContent).toContain('customer-lines Works!');
    });

});
