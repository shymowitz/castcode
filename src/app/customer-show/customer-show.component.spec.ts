import { TestBed } from '@angular/core/testing';

import { CustomerShowComponent } from './customer-show.component';

describe('customer-show Component', () => {
    beforeEach(() => {
        TestBed.configureTestingModule({
            declarations: [CustomerShowComponent]
        });
    });

    it('should be fixed becaue right now it doesn\'t test anything', () => {
        // const fixture = TestBed.createComponent(CustomerShowComponent); // we need to come back and fix this(102816)
        // fixture.detectChanges();
        // expect(fixture.nativeElement.children[0].textContent).toContain('customer-info Works!');
    });

});
